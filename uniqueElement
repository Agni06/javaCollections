int distinct(int[] a)
{

    int count =0;
    for(int i=1;i<a.length;i++)
    {
        int j = i-1;
        while(j>=0)
        {
            if(a[i]==a[j])
            {
                break;
            }

            
        }
        count ++;
    }
    return count;
}
